package com.zalando.onboarding.flow;

/** How a field is captured. Rendering is the client's business; this is the shape contract. */
public enum FieldType {
    TEXT,
    DATE,
    BOOLEAN,
    CONSENT
}
