package com.zalando.onboarding.domain;

/**
 * Markets open for self-service onboarding. Country selects the flow definition and is
 * immutable once the application exists.
 */
public enum Country {
    DE,
    PL,
    NL
}
